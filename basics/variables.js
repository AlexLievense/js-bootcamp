// 1. You can't define a variable more than once.
let petName = 'Binky';
petName = 'Spot';

console.log(petName);

// 2. There are rules related to variable names.
// 2.1  variable name has to start with a letter, _ or $.

// 3. Variable names cannot be resered keywords.